#include "SolarSystem.h"

int main(int argc, char* argv[])
{
	SolarSystem* game = new SolarSystem(argc, argv); //Creates an instance of our game

	return 0; //Assumes a successful exit if our game exists
}